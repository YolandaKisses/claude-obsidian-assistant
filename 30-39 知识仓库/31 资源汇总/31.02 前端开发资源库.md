---
title: 前端开发资源库
tags: [前端, Vue, 开发资源, 代码片段]
---
# 前端开发资源库

## 一、项目技术栈官方文档
### 核心框架
- [Vue 2 官方文档](https://v2.cn.vuejs.org/)
- [Vuex 3.x 文档](https://v3.vuex.vuejs.org/zh/)
- [Vue Router 3.x 文档](https://v3.router.vuejs.org/zh/)
### UI组件
- [Element UI 2.x 文档](https://element.eleme.cn/#/zh-CN)
- [VXE Table 2.x 文档](https://vxetable.cn/v2/#/table/start/install)
### 可视化
- [ECharts 5.x 文档](https://echarts.apache.org/zh/api.html)
- [AntV X6 文档](https://x6.antv.vision/zh/docs/tutorial/getting-started)
- [BPMN-js 文档](https://bpmn.io/toolkit/bpmn-js/)
- [Camunda 流程引擎文档](https://docs.camunda.org/manual/latest/)

## 二、常用工具库
### Excel处理
- [XLSX 文档](https://docs.sheetjs.com/) - Excel导入导出核心库
- [FileSaver 文档](https://github.com/eligrey/FileSaver.js/) - 文件下载工具
### 通用工具
- [Lodash 文档](https://www.lodashjs.com/) - 工具函数库
- [Moment.js 文档](https://momentjs.cn/) - 时间处理工具
- [Axios 文档](https://axios-http.com/zh/docs/intro) - 网络请求库
### 安全加密
- [CryptoJS 文档](https://cryptojs.gitbook.io/docs/) - 加密解密工具

## 三、常用代码片段
### 1. 防抖函数
```javascript
function debounce(func, wait = 300) {
  let timeout
  return function() {
    clearTimeout(timeout)
    timeout = setTimeout(() => func.apply(this, arguments), wait)
  }
}
```
### 2. Excel导出通用配置
```javascript
import XLSX from 'xlsx'
import FileSaver from 'file-saver'

// 导出Excel
export function exportExcel(data, fileName = '导出文件') {
  const ws = XLSX.utils.json_to_sheet(data)
  const wb = XLSX.utils.book_new()
  XLSX.utils.book_append_sheet(wb, ws, 'Sheet1')
  const wbout = XLSX.write(wb, { bookType: 'xlsx', type: 'array' })
  FileSaver.saveAs(new Blob([wbout]), `${fileName}.xlsx`)
}
```
### 3. ECharts通用销毁逻辑
```javascript
export default {
  beforeDestroy() {
    if (this.chartInstance) {
      this.chartInstance.dispose()
      this.chartInstance = null
    }
  }
}
```
### 4. 时间格式化工具
```javascript
import moment from 'moment'

// 格式化日期 YYYY-MM-DD
export function formatDate(date) {
  return moment(date).format('YYYY-MM-DD')
}

// 格式化日期时间 YYYY-MM-DD HH:mm:ss
export function formatDateTime(date) {
  return moment(date).format('YYYY-MM-DD HH:mm:ss')
}
```

## 四、规范文档
- [平安基金前端开发规范](https://内网规范地址)
- [接口联调规范](https://内网规范地址)
- [Git提交规范](https://内网规范地址)
