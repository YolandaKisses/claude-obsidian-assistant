---
project: "DataDriver Fund QTrade Python"
status: active
type: context-notes
updated: 2026-04-20
source_note: "[[11.04 DataDriver Fund QTrade Python]]"
---

# DataDriver Fund QTrade Python 上下文记录

## 使用约定

- 进入项目后，先读取项目主笔记 `11.04 DataDriver Fund QTrade Python` 作为基线。
- 再读取本笔记，作为最近协作状态与连续上下文补充。
- 每次有意义的项目对话结束后，默认追加一条记录。
- 追加内容固定覆盖：主题、结论、涉及范围、影响面、验证、风险、下一步。
- 如果某次只是临时讨论且不需要沉淀，可在当次对话中显式跳过。

## 2026-04-20

### 会话主题
- 建立 Python 项目的 Codex 协作约定：进入项目后先读取主 Obsidian 笔记，再读取项目上下文记录作为最近协作状态。
- 参考 `datadriver-fund-qtrade-vue` 项目的 `AGENTS.md` 与上下文记录格式，对当前仓库同步升级。

### 已确认结论 / 决策
- 后续进入本项目时，默认先读取 `11.04 DataDriver Fund QTrade Python.md` 作为项目基线。
- 在读取主笔记后，默认继续读取 `11.04 DataDriver Fund QTrade Python 上下文记录.md` 作为最近协作状态来源。
- 每次有意义的项目对话结束后，默认向同一篇 Obsidian 笔记追加一条上下文记录，而不是覆盖历史内容。
- 记录内容优先覆盖：主题、结论、涉及范围、影响面、验证、风险、下一步。

### 本次变更 / 涉及范围
- 更新当前仓库内 `AGENTS.md`，补充“主笔记 + 上下文记录”的读取顺序与写回规则。
- 新建 Python 项目的 Obsidian 上下文记录笔记，用作后续持续追加的统一落点。

### 涉及文件 / 模块
- `AGENTS.md`
- `11.04 DataDriver Fund QTrade Python 上下文记录.md`

### API / 数据流影响
- 本次仅调整协作约定与项目知识沉淀机制。
- 不涉及 Django 接口、服务层逻辑、任务调度、数据库结构或前后端数据流变更。

### 验证情况
- 已确认当前项目主笔记 `11.04 DataDriver Fund QTrade Python.md` 可正常读取。
- 已参考 `11.02 DataDriver Fund QTrade Vue` 项目的 `AGENTS.md` 与上下文记录格式完成规则对齐。
- 已在当前仓库 `AGENTS.md` 中写入新的读取与记录规则。
- 已创建 Python 项目的 Obsidian 上下文记录文件并写入首条记录。

### 风险 / 待办
- 当前协作约定已落地，但后续仍需保持每次有意义对话后及时追加记录，避免上下文笔记再次失去时效性。
- 如果后续希望将“验证”字段细化为测试命令、接口联调结果、截图或日志，也可以继续增强模板。

### 下一步建议
- 从下一次开始，只要形成稳定结论、明确改动或阶段性结果，我就默认追加上下文记录。
- 如果某次你只想临时讨论、不希望写入 Obsidian，可以在当次对话里明确说明，我就跳过落盘。
